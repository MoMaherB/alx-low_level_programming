#include "main.h"
#include <string.h>

/**
 * _strlen - function that takes a pointer to an int parametr
 *
 *@s: ...do somthing
 */

int _strlen(char *s);
{
	int strl;
	strl = strlen(char *s);
	return (strl);

}
