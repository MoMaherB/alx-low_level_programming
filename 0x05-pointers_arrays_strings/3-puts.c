#include "main.h"

/**
 * _puts - function that takes a pointer to an int parametr
 *
 *@str: ...do somthing
 *
 * Return: length
 */

void _puts(char *str);
{
	while (*str !='\0')
	{
		_putchar(*str + 0)'
			str++;
	}
	_putchar('\n');
}
